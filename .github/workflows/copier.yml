name: Ultra Realtime Copier HA

on:
  workflow_dispatch:
  schedule:
    - cron: "*/5 * * * *"

concurrency:
  group: copier-ha
  cancel-in-progress: true

jobs:
  run-copier:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:

      # =========================
      # CHECKOUT
      # =========================
      - name: ⬇ Checkout repository
        uses: actions/checkout@v4

      # =========================
      # PYTHON SETUP
      # =========================
      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      # =========================
      # DEPENDENCIES
      # =========================
      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install telethon pyyaml

      # =========================
      # VALIDATE SECRETS
      # =========================
      - name: 🔎 Validate secrets presence
        env:
          API_ID: ${{ secrets.API_ID }}
          API_HASH: ${{ secrets.API_HASH }}
          SESSION_STRING: ${{ secrets.SESSION_STRING }}
        run: |
          echo "API_ID present: ${API_ID:+YES}"
          echo "API_HASH present: ${API_HASH:+YES}"
          echo "SESSION_STRING present: ${SESSION_STRING:+YES}"

      # =========================
      # TELEGRAM SESSION TEST
      # =========================
      - name: 🧪 Test Telegram session
        env:
          API_ID: ${{ secrets.API_ID }}
          API_HASH: ${{ secrets.API_HASH }}
          SESSION_STRING: ${{ secrets.SESSION_STRING }}
        run: |
          python - << 'PY'
from telethon.sync import TelegramClient
from telethon.sessions import StringSession
import os

api_id = int(os.getenv("API_ID"))
api_hash = os.getenv("API_HASH")
session = os.getenv("SESSION_STRING")

print("Starting Telegram diagnostic...")

with TelegramClient(StringSession(session), api_id, api_hash) as client:
    me = client.get_me()
    print("CONNECTED:", me.username, me.id)
PY

      # =========================
      # RUN BOT
      # =========================
      - name: 🚀 Run Copier
        env:
          API_ID: ${{ secrets.API_ID }}
          API_HASH: ${{ secrets.API_HASH }}
          SESSION_STRING: ${{ secrets.SESSION_STRING }}
        run: |
          echo "▶ Launching copier.py"
          python copier.py

      # =========================
      # UPLOAD LOGS / DB
      # =========================
      - name: 📤 Upload logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: copier-artifacts
          path: |
            *.log
            *.db
