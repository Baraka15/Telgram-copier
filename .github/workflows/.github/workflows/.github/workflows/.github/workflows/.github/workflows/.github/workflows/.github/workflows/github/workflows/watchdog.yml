name: Watchdog Restart
on:
  workflow_run:
    workflows: ["Bot Runner"]
    types: [completed]

jobs:
  retrigger:
    runs-on: ubuntu-latest
    steps:
      - name: Restart Bot Runner
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh workflow run copier.yml
